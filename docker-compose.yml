services:
  postgres:
    image: postgres:17
    container_name: postgres-paid
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: postgres-paid
    ports:
      - "5666:5432"
    volumes:
      - postgres-paid-data:/var/lib/postgresql/data
    networks:
      - postgres-paid-network
    command: postgres -c 'max_connections=100'
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d postgres-paid"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s

volumes:
  postgres-paid-data:
    driver: local

networks:
  postgres-paid-network:
    name: postgres-paid-network
    driver: bridge
